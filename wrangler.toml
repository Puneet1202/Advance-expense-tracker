name = "expense-tracker"
main = "backend/src/app.js"
compatibility_date = "2026-04-28"
compatibility_flags = [ "nodejs_compat" ]

# Dev server
[dev]
port = 8787

# NOTE: D1 aur Vectorize hata diya — backend ab Supabase use karta hai
# Secrets .dev.vars se aate hain (root folder mein):
# ACCESS_TOKEN_SECRET, REFRESH_TOKEN_SECRET, EXCHANGE_RATE_API_KEY, RESEND_API_KEY, GEMINI_API_KEY